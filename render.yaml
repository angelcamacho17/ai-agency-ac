services:
  - type: web
    name: ai-agency-ac
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist/ai-agency-ac/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
