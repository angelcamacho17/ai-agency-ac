# Angular 21 packages are all on 21.0.7, but npm's strict peer resolver tries to
# pull the "latest 21.x" for transitive peers (e.g. @angular/ssr) and reports a
# false conflict. The installed tree is internally consistent, so allow legacy
# peer resolution. Required for clean `npm ci` on Render and other CI hosts.
legacy-peer-deps=true
